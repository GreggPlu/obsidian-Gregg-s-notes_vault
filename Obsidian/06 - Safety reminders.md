• 	GitHub is a versioned backup, but I still keep my removable‑drive backups for full redundancy.
• 	Only committed files are backed up — if I forget to commit, those changes won’t be on GitHub.
• 	If something breaks, I can always run Git commands manually in Git Bash inside the vault folder.